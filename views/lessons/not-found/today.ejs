<% let np = schedule.nextParams -%>
Сьогодні, у <%= localizer.day(np.day).on.toLowerCase() %> <%= np.date.date() %> <%= localizer.month(np.month).when.toLowerCase() %>, у групи <%=schedule.name%> пар немає.